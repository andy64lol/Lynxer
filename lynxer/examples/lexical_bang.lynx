// A bare `!` is not valid in Clynxer; use `not` or `!!`. The Python reference
// accepts `!value`, which makes this a deliberate lexical divergence.
global setup() {}

global main() {
    println(!true);
}
