name: Build Lynxer on ARM64

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    name: Build and Test Lynxer on ARM64
    runs-on: ubuntu-24.04-arm
    timeout-minutes: 60

    steps:
      - uses: actions/checkout@v4

      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Rust dependencies
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: lynxer/rust -> ../build/rust

      - name: Set up Python 3.14
        uses: actions/setup-python@v5
        with:
          python-version: "3.14"

      - name: Install ARM64 toolchain
        run: sudo apt-get update && sudo apt-get install -y gcc-aarch64-linux-gnu libasound2-dev pkg-config

      - name: Build Lynxer for ARM64
        run: make buildLynxerArm64

      - name: Test Lynxer ARM64 binary
        run: make testLynxer LYNXER_SKIP_DISPLAY=1
      - name: Run the ARM64 syscall fixture
        run: make testLynxerArm64Syscalls
      - name: Upload ARM64 binary
        uses: actions/upload-artifact@v4
        with:
          name: lynxer-arm64
          path: lynxer/lynxer
