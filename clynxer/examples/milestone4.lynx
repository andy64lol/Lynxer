global setup(){}

global main(){
    int left = 6;
    int right = 3;
    println(left bitand right);
    println(left bitor right);
    println(left bitxor right);
    println(left bitnand right);
    println(left bitxnor right);
    println(left bitnor right);
    println(bitnot left);
    println(right bitleft 2);
    println(16 bitright 2);
    println(2 ** 5);
    println(-7 /% right);
    bool yes = true;
    bool no = false;
    println(yes nand yes);
    println(no nor no);
    println(yes and not no);

    if(false){
        println("wrong");
    } elif(true){
        println("elif");
    } else {
        println("wrong");
    }

    int selected = 2;
    switch(selected){
        case(1){ println("wrong"); }
        case(2){ println("case"); }
        default(){ println("wrong"); }
    }

    try {
        intOf("not an integer");
    } catch(str error) {
        println("caught: " + error);
    }
    println("\e");
}
