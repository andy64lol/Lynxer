global setup(){
    overrideMain("start");
}

func add(int left, int right = 2) -> int {
    return left + right;
}

global start(){
    local square(int value) -> int {
        return value * value;
    }
    println(add(3));
    println(add(3, 4));
    println(local.square(5));
}
