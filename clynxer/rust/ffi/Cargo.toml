[package]
name = "clynxer_ffi"
version = "0.1.0"
edition = "2021"
description = "Rust backend for the Clynxer `ffi` built-in family"

[lib]
name = "clynxer_ffi"
crate-type = ["cdylib"]

[dependencies]
clynxer_abi = { path = "../abi" }
libffi = "5"
